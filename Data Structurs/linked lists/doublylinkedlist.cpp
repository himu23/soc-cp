//every node points to its next nad previous node;
//this provides us benefits like traversing back and forth, but it takes extra space;
//the previous pointer of head and the next pointer of tail is null;
/*uses 
    Implementing LRU (Least Recently Used) cache algorithm
    Used in applications where undo redo use-cases are applicable
    Deletion operation in O(1) time using map.
*/
