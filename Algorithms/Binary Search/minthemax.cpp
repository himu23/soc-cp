//minimize the maximize
//q1 d1  //q4 d3
//If every element is ≤x, then it must also be true that Pi≤i⋅x for every i.
//ceil(a/b)=(a+b-1)/b
#include <bits/stdc++.h>
using namespace std;
#define ll long long int

int main() {
    cin.tie(0)->sync_with_stdio(false);
    
    return 0;
}
